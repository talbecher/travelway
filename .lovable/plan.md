## Plan

### 1. פורמט אחיד "בוקר ב: {מלון}"
- ב-`src/lib/hotels.ts`: שינוי `morningTitle` מ-`בוקר ב${name}` ל-`בוקר ב: ${name}` (כולל ב-`titleVariants` למחיקה, כדי שכניסות ישנות בפורמט הקודם ימחקו וייווצרו מחדש בפורמט החדש בסנכרון הבא).

### 2. כניסת "נווט אל" במסלול היומי
ב-`src/routes/itinerary.$dayId.tsx`:
- הוספת כפתור/פעולה חדשה "נווט אל" (ליד ה-Quick Note / חיפוש המקומות הקיים).
- לחיצה פותחת `BottomSheet` עם:
  - `PlacesSearch` לבחירת יעד מגוגל מפות (ממלא name/address/coords/google_maps_url/photo).
  - שדה טקסט חופשי אופציונלי (למשל "לאסוף מזוודות בדרך") שיישמר כ-notes.
  - כפתור "הוסף".
- שמירה: הוספת שורה ל-`day_entries` עם:
  - `entry_type: "activity"` (הסוג הקיים הכללי במערכת)
  - `icon_emoji: "🧭"`, `title: "נווט אל: {name}"` (או הטקסט החופשי אם אין מקום)
  - `location_name`, `latitude`, `longitude`, `google_maps_url`, `photo_url` מהמקום שנבחר
  - `notes` = הטקסט החופשי
  - `display_order` בסוף היום
- בכרטיס הכניסה הקיים, ה-`google_maps_url` כבר משמש לניווט — אין שינוי נוסף בתצוגה.

### פרטים טכניים
- לא נדרש שינוי סכמה — כל השדות קיימים ב-`day_entries`.
- הפורמט "בוקר ב: " יגרום ל-titleVariants הישן (`בוקר ב${n}`) להישאר עד סנכרון הבא; לכן נכלול בגרסת המחיקה **גם** את הפורמט הישן וגם החדש כדי לנקות בפעם הראשונה שמלון מסתנכרן מחדש.
