## תיקון נקודתי לטיול של yuvalkalishh

**מה מצאתי בבסיס הנתונים:**
- הטיול: `Japan 2026` (owner: yuvalkalishh@gmail.com)
- `start_date` בטבלת `trips` כבר תקין: **2026-11-17** ו-`end_date`: **2026-12-09**
- אבל בטבלת `itinerary_days` הימים ממוספרים **2..23** (ה-`day_number=1` נמחק כשתיקנו את הבאג), והתאריכים רצים 17.11 → 08.12 — כך שהיום הראשון שהיא רואה במסלול מופיע כ"יום 2 · 17.11" ולא "יום 1 · 17.11". בנוסף חסר יום אחד בסוף (09.12) כדי לכסות את כל טווח הטיול.

**התיקון (migration אחד, נתונים בלבד, ללא שינוי סכמה):**

1. הקטנה של `day_number` ב-1 לכל השורות של הטיול — הימים הקיימים יהפכו ל-1..22, כשהראשון = 17.11.
2. הוספת יום חדש בסוף: `day_number=23`, `date=2026-12-09`, `city_label=NULL` — כדי לסגור את הטווח עד end_date.
3. הפעולה עטופה ב-`BEGIN/COMMIT` ומוגבלת ל-`trip_id = '845ca94b-8cfb-460f-99ff-cd0dbd0ae438'` בלבד.

**מה לא נוגעים בו:**
- `trips.start_date` / `end_date` — כבר נכונים.
- שום `day_entries` / hotels / expenses — כולם מקושרים ל-`day_id` (UUID), לא ל-`day_number`, אז שינוי המספור שקוף להם.
- שום קוד או טיול של משתמש אחר.

**אימות אחרי הרצה:** `SELECT day_number, date FROM itinerary_days WHERE trip_id=... ORDER BY day_number` — נצפה 1→17.11, 23→09.12.
